---
phase: quick-260409-e12
plan: "01"
subsystem: ai-pipeline
tags: [llm, routing, misconceptions, extraction, semantic-fallback]
dependency_graph:
  requires: []
  provides: [15-domain extraction, semantic-fallback routing, all-concepts routing, best-match diagnosis]
  affects: [packages/llm, packages/router, apps/web/app/api/ask]
tech_stack:
  added: [zod in @mindmap/router, ai in @mindmap/router]
  patterns: [batched LLM fallback with graceful degradation, name-to-ID map for safe concept lookup]
key_files:
  created:
    - packages/router/src/semantic-fallback.ts
  modified:
    - packages/llm/src/prompts/extract.ts
    - packages/llm/src/__tests__/prompts.test.ts
    - packages/router/src/index.ts
    - packages/router/src/__tests__/router.test.ts
    - packages/router/package.json
    - apps/web/app/api/ask/route.ts
decisions:
  - "DOMAINS constant exported from extract.ts for reuse by semantic-fallback prompt"
  - "semanticFallback accepts model as parameter (Option 2) — keeps router stateless, no @mindmap/llm dep in @mindmap/router"
  - "Merged routingDecisions and semanticMatches into mergedDecisions before the dedup loop so the loop iterates the final set"
  - "conceptNameToResolvedId Map built during dedup loop; diagnose branch looks up by name not array index"
  - "T-e12-01: getMisconceptionById validates LLM-returned misconceptionId before session creation"
metrics:
  duration_seconds: 229
  completed_date: "2026-04-09"
  tasks_completed: 2
  files_changed: 7
---

# Quick Task 260409-e12: Fix MindMap Core AI Engine Summary

**One-liner:** Fixed four AI pipeline bugs: 15-domain Zod enum with few-shot extraction prompt, batched LLM semantic fallback in @mindmap/router, all-concepts routing with highest-confidence diagnosis selection, and name-keyed concept ID map replacing unsafe array-index-0 lookup.

---

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Expand domain enum to 15 and improve extraction prompt | 829cc3d | extract.ts, prompts.test.ts |
| 2 | Add semantic fallback and fix all-concepts routing | 4552c79 | semantic-fallback.ts, router/index.ts, router.test.ts, router/package.json, ask/route.ts |

---

## What Was Built

### Task 1 — Domain Enum Expansion + Extraction Prompt

**`packages/llm/src/prompts/extract.ts`**
- `DOMAINS` constant (15 values) exported for reuse
- `z.enum(DOMAINS)` replaces the old 5-value inline enum
- `buildExtractPrompt` rewritten: "2-4 concepts" guidance, full 15-domain list in prompt text, two XML-tagged few-shot examples (leaves/autumn, moon phases) each showing Good vs Bad extraction
- Tests updated: chemistry now valid, "cooking" rejects, all-15-domains loop test, prompt content tests for "2-4", `<example>`, "computer-science", "astronomy"

### Task 2 — Semantic Fallback + All-Concepts Routing Fix

**`packages/router/src/semantic-fallback.ts`** (new)
- `semanticFallback(unmatched, gradeLevel, model)` — returns `[]` immediately for empty input (no LLM call)
- Loads full misconception library, filters by grade band
- Single batched `generateText` call with `Output.object` for structured output
- `try/catch` wraps entire body — any error logs a warning and returns `[]` (graceful degradation to enrich mode)
- `SemanticMatch` type exported alongside function

**`packages/router/src/index.ts`**
- Re-exports `semanticFallback` and `SemanticMatch` from `./semantic-fallback`
- Existing `routeQuestion` unchanged

**`apps/web/app/api/ask/route.ts`**
- Import updated: `routeQuestion, semanticFallback, type RoutingDecision` from `@mindmap/router`
- Routing pipeline now: string match all concepts → collect unmatched → single semantic fallback call → merge into `mergedDecisions` → sort diagnose decisions by probability → pick highest confidence as `primaryDecision`
- Dedup loop iterates `mergedDecisions` (not `routingDecisions`)
- `conceptNameToResolvedId` Map built during loop
- Diagnose branch: looks up `diagConceptId` via `conceptNameToResolvedId.get(primaryDiagnoseConcept.name)` — never uses `resolvedConceptIds[0]`
- `getMisconceptionById` validates LLM-returned ID before session creation (T-e12-01 mitigation)

---

## Verification Results

```
@mindmap/llm: 29 tests passed (2 test files)
@mindmap/router: 17 tests passed (1 test file)
web build: clean — no TypeScript errors
```

Manual checks:
- `routingDecisions[0]` no longer appears in route.ts
- `resolvedConceptIds[0]` no longer appears in route.ts

---

## Deviations from Plan

None — plan executed exactly as written.

The advisory note about using actual monorepo versions (`ai: "^6.0.154"`, `zod: "^3.25.76"`) instead of the stale plan versions (`ai: "^4.3.16"`, `zod: "^3.24.4"`) was applied as instructed.

---

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced.

The semantic fallback sends misconception library content (educational text, not PII) to the Claude API — already documented as T-e12-04 (accepted) in the plan's threat model. PRIV-01 maintained: no student data in the fallback prompt.

T-e12-01 mitigation confirmed: `getMisconceptionById(primaryDecision.misconceptionId)` validates every LLM-returned ID against the Git-controlled YAML library before session creation. Invalid IDs fall through to enrich mode.

---

## Known Stubs

None.

---

## Self-Check

### Files exist:
- packages/router/src/semantic-fallback.ts: FOUND
- packages/llm/src/prompts/extract.ts: FOUND (modified)
- apps/web/app/api/ask/route.ts: FOUND (modified)

### Commits exist:
- 829cc3d: FOUND
- 4552c79: FOUND

## Self-Check: PASSED
