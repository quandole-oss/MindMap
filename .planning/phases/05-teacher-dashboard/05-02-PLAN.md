---
phase: 05-teacher-dashboard
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/app/teacher/classes/[classId]/dashboard/page.tsx
  - apps/web/components/dashboard/dashboard-tabs.tsx
  - apps/web/components/dashboard/overview-tab.tsx
  - apps/web/components/dashboard/students-tab.tsx
  - apps/web/components/layout/sidebar.tsx
  - apps/web/app/teacher/page.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Teacher can navigate to a class dashboard from the sidebar or class list"
    - "Teacher sees an Overview tab with class totals (students, questions, active misconceptions, avg breadth)"
    - "Teacher sees a Students tab with per-student rows showing name, last active, streak, breadth score, inactivity badge, and a mini graph thumbnail"
    - "Student graph thumbnails render correctly per the static SVG approach"
    - "Inactive students (3+ days) show an amber badge"
  artifacts:
    - path: "apps/web/app/teacher/classes/[classId]/dashboard/page.tsx"
      provides: "Dashboard page with server data fetching"
      min_lines: 20
    - path: "apps/web/components/dashboard/dashboard-tabs.tsx"
      provides: "Tab navigation component"
      exports: ["DashboardTabs"]
    - path: "apps/web/components/dashboard/overview-tab.tsx"
      provides: "Overview summary cards"
      exports: ["OverviewTab"]
    - path: "apps/web/components/dashboard/students-tab.tsx"
      provides: "Students table with graph thumbnails"
      exports: ["StudentsTab"]
    - path: "apps/web/components/layout/sidebar.tsx"
      provides: "Updated sidebar with class dashboard links"
  key_links:
    - from: "apps/web/app/teacher/classes/[classId]/dashboard/page.tsx"
      to: "apps/web/actions/dashboard.ts"
      via: "server action call"
      pattern: "getClassDashboardData"
    - from: "apps/web/components/dashboard/students-tab.tsx"
      to: "apps/web/components/graph/mini-graph-svg.tsx"
      via: "import and render per student"
      pattern: "MiniGraphSvg"
    - from: "apps/web/components/layout/sidebar.tsx"
      to: "/teacher/classes/[classId]/dashboard"
      via: "navigation link"
      pattern: "dashboard"
---

<objective>
Build the dashboard page, tab navigation, Overview tab, Students tab with graph thumbnails, and sidebar navigation updates.

Purpose: Teachers need a single entry point to view class-wide summary stats and per-student detail. This plan creates the page shell, tab system, the Overview and Students tabs, and wires navigation.

Output: Dashboard route at `/teacher/classes/[classId]/dashboard`, tab-based layout, Overview stats cards, Students table with mini graphs + engagement metrics, sidebar links per class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-teacher-dashboard/05-CONTEXT.md
@.planning/phases/05-teacher-dashboard/05-01-SUMMARY.md

@apps/web/app/teacher/page.tsx
@apps/web/app/teacher/classes/[classId]/roster/page.tsx
@apps/web/components/layout/sidebar.tsx

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From apps/web/lib/dashboard-types.ts:
```typescript
export interface StudentSummary {
  studentId: string;
  studentName: string | null;
  studentEmail: string | null;
  gradeLevel: number;
  lastQuestionDate: Date | null;
  streak: number;
  totalQuestions: number;
  uniqueDomains: number;
  totalDomains: number;
  breadthScore: number;       // 0-1
  isInactive: boolean;        // 3+ days no question
  graph: StudentGraphData;
}

export interface ClassDashboardData {
  classInfo: { id: string; name: string; joinCode: string; gradeLevel: number };
  students: StudentSummary[];
  conceptHeatmap: ConceptHeatmapEntry[];
  misconceptionClusters: MisconceptionCluster[];
  totals: {
    totalStudents: number;
    totalQuestions: number;
    activeMisconceptions: number;
    avgBreadthScore: number;
  };
}
```

From apps/web/actions/dashboard.ts:
```typescript
export async function getClassDashboardData(classId: string): Promise<ClassDashboardData | { error: string }>;
```

From apps/web/components/graph/mini-graph-svg.tsx:
```typescript
export function MiniGraphSvg(props: { nodes: ...; edges: ...; width?: number; height?: number; className?: string }): JSX.Element;
```

From apps/web/components/layout/sidebar.tsx:
```typescript
interface SidebarProps {
  role: "student" | "teacher";
  userName?: string | null;
  streak?: number;
}
// teacherNavItems is a static array — needs to become dynamic for class dashboard links
```

From apps/web/actions/class.ts:
```typescript
export async function getTeacherClasses(): Promise<Array<{ id, name, joinCode, teacherId, gradeLevel, createdAt }>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard page, tab component, and Overview tab</name>
  <files>apps/web/app/teacher/classes/[classId]/dashboard/page.tsx, apps/web/components/dashboard/dashboard-tabs.tsx, apps/web/components/dashboard/overview-tab.tsx</files>
  <read_first>
    - apps/web/app/teacher/classes/[classId]/roster/page.tsx (pattern for classId dynamic route + auth)
    - apps/web/actions/dashboard.ts (getClassDashboardData signature from Plan 01)
    - apps/web/lib/dashboard-types.ts (ClassDashboardData type from Plan 01)
    - apps/web/components/ui/card.tsx (existing Card component)
    - apps/web/components/ui/badge.tsx (existing Badge component)
  </read_first>
  <action>
**Step 1: Create `apps/web/app/teacher/classes/[classId]/dashboard/page.tsx`** (server component).

Follow the same pattern as the roster page:
- Props: `{ params: Promise<{ classId: string }> }`
- Await params, call `getClassDashboardData(classId)`
- If result has `"error"` key, call `notFound()`
- Render page with `<h1>{data.classInfo.name} -- Dashboard</h1>` and `<DashboardTabs data={data} />`
- Import `notFound` from `next/navigation`

**Step 2: Create `apps/web/components/dashboard/dashboard-tabs.tsx`** (`"use client"`).

This is the tab navigation shell. Use `useState` for active tab (default: "overview"). Do NOT use shadcn Tabs — build a simple custom tab bar (per Claude's Discretion from CONTEXT.md).

```
interface DashboardTabsProps {
  data: ClassDashboardData;
}
```

Tab bar: horizontal row of 4 buttons — "Overview", "Concepts", "Misconceptions", "Students". Active tab gets `bg-[#18181b] text-white`, inactive gets `text-[#52525b] hover:bg-[#e4e4e7]`. Tailwind classes matching existing sidebar active/inactive pattern.

Conditionally render tab content:
- "overview" → `<OverviewTab totals={data.totals} />`
- "concepts" → `<div>Concepts tab placeholder</div>` (Plan 03 will replace this)
- "misconceptions" → `<div>Misconceptions tab placeholder</div>` (Plan 03 will replace)
- "students" → `<StudentsTab students={data.students} />`

**Important**: Import `OverviewTab` and `StudentsTab`. For "concepts" and "misconceptions", render a simple `<p className="text-[14px] text-[#71717a] py-8">Coming soon</p>` placeholder. Plan 03 will create the real components and update the imports.

**Step 3: Create `apps/web/components/dashboard/overview-tab.tsx`** (no `"use client"` needed — pure props).

Receives `{ totals: ClassDashboardData["totals"] }`.

Render 4 stat cards in a `grid grid-cols-2 lg:grid-cols-4 gap-4` layout using the existing `Card` component:
1. **Students**: `totals.totalStudents` with `Users` lucide icon
2. **Questions Asked**: `totals.totalQuestions` with `MessageSquare` lucide icon
3. **Active Misconceptions**: `totals.activeMisconceptions` with `AlertCircle` lucide icon (coral text if > 0)
4. **Avg. Breadth**: `${Math.round(totals.avgBreadthScore * 100)}%` with `Compass` lucide icon

Each card: Card component with padding, icon top-left, large number, small label underneath. Style matching the existing app (text-[20px] font-semibold for numbers, text-[12px] text-[#71717a] for labels).
  </action>
  <verify>
    <automated>cd /Users/q/MindMap && npx tsc --noEmit apps/web/app/teacher/classes/\\[classId\\]/dashboard/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getClassDashboardData" apps/web/app/teacher/classes/\[classId\]/dashboard/page.tsx
    - grep -q "DashboardTabs" apps/web/components/dashboard/dashboard-tabs.tsx
    - grep -q "use client" apps/web/components/dashboard/dashboard-tabs.tsx
    - grep -q "OverviewTab" apps/web/components/dashboard/overview-tab.tsx
    - grep -q "totalStudents" apps/web/components/dashboard/overview-tab.tsx
    - grep -q "avgBreadthScore" apps/web/components/dashboard/overview-tab.tsx
    - grep -q "overview" apps/web/components/dashboard/dashboard-tabs.tsx
    - grep -q "students" apps/web/components/dashboard/dashboard-tabs.tsx
    - grep -q "concepts" apps/web/components/dashboard/dashboard-tabs.tsx
    - grep -q "misconceptions" apps/web/components/dashboard/dashboard-tabs.tsx
  </acceptance_criteria>
  <done>
    - Dashboard page at /teacher/classes/[classId]/dashboard renders with fetched data
    - Tab navigation shows 4 tabs: Overview, Concepts, Misconceptions, Students
    - Overview tab displays 4 stat cards with real data from totals
    - Active tab visually highlighted, content switches on click
    - Concepts and Misconceptions tabs show placeholder (Plan 03 replaces)
  </done>
</task>

<task type="auto">
  <name>Task 2: Students tab with mini graphs + sidebar and class list navigation updates</name>
  <files>apps/web/components/dashboard/students-tab.tsx, apps/web/components/layout/sidebar.tsx, apps/web/app/teacher/page.tsx</files>
  <read_first>
    - apps/web/components/graph/mini-graph-svg.tsx (MiniGraphSvg from Plan 01 Task 2)
    - apps/web/lib/dashboard-types.ts (StudentSummary type)
    - apps/web/components/layout/sidebar.tsx (current sidebar to modify)
    - apps/web/app/teacher/page.tsx (current teacher class list to add dashboard links)
    - apps/web/components/ui/badge.tsx (Badge component)
    - apps/web/components/ui/table.tsx (Table component)
    - apps/web/actions/class.ts (getTeacherClasses)
  </read_first>
  <action>
**Step 1: Create `apps/web/components/dashboard/students-tab.tsx`** (`"use client"` not needed — pure props).

Props: `{ students: StudentSummary[] }`.

Render a table (using the existing shadcn Table component from `@/components/ui/table`) with columns:

| Column | Content |
|--------|---------|
| Graph | `<MiniGraphSvg nodes={student.graph.nodes} edges={student.graph.edges} width={80} height={60} />` |
| Student | `student.studentName ?? student.studentEmail ?? "Unknown"` |
| Last Active | Format `student.lastQuestionDate` as relative time ("2 days ago", "Never") + amber `<Badge>` with text "Inactive" if `student.isInactive` is true |
| Streak | `student.streak` days (show flame emoji only if > 0) |
| Breadth | `${Math.round(student.breadthScore * 100)}%` with a small inline bar (div with `bg-[#14b8a6]` width proportional to breadthScore, max 60px) |
| Questions | `student.totalQuestions` |

Sort students: inactive first (amber alert visibility), then by last question date descending.

If `students.length === 0`, render "No students enrolled yet" empty state (same pattern as roster page).

For relative time formatting: compute days since lastQuestionDate. If null, "Never". If 0, "Today". If 1, "Yesterday". Else `${days} days ago`. Do NOT use a library — simple math is fine.

**Step 2: Update `apps/web/components/layout/sidebar.tsx`**.

The sidebar currently has a static `teacherNavItems` array. The teacher needs to see their classes listed as dashboard links.

Changes:
1. Add a new prop `classes` to `SidebarProps`: `classes?: Array<{ id: string; name: string }>` (optional, defaults to empty array).
2. Below the static teacher nav items, add a "Classes" section heading (`<p className="px-4 pt-4 pb-1 text-[11px] font-medium uppercase tracking-wider text-[#a1a1aa]">Classes</p>`).
3. Map `classes` to Link items: `href={/teacher/classes/${cls.id}/dashboard}`, label = `cls.name`, icon = `BookOpen` (from lucide-react). Same active/inactive styling as other nav items.
4. Keep existing `teacherNavItems` ("My Classes", "Create a Class") above the classes list.

**Step 3: Update `apps/web/app/teacher/page.tsx`**.

The teacher class list currently links each class card to `/teacher/classes/${cls.id}/roster`. Change the link destination to `/teacher/classes/${cls.id}/dashboard` instead. Add a secondary "View roster" link inside the card that still goes to the roster page.

Specifically:
- Main card `<Link>` href changes from `/teacher/classes/${cls.id}/roster` to `/teacher/classes/${cls.id}/dashboard`
- Add text below the badges: `<span className="text-[12px] text-[#71717a]">Open dashboard</span>`
- The "View roster -->" text on the right stays but change href to `/teacher/classes/${cls.id}/roster` as a separate small link (not the main card link)

**Step 4: Update sidebar consumption** — find the layout file that renders `<Sidebar>` and pass the `classes` prop for teachers. Check `apps/web/app/teacher/layout.tsx` or similar. The layout should call `getTeacherClasses()` and pass the result as `classes` prop.
  </action>
  <verify>
    <automated>cd /Users/q/MindMap && npx tsc --noEmit apps/web/components/dashboard/students-tab.tsx apps/web/components/layout/sidebar.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "StudentsTab" apps/web/components/dashboard/students-tab.tsx
    - grep -q "MiniGraphSvg" apps/web/components/dashboard/students-tab.tsx
    - grep -q "isInactive" apps/web/components/dashboard/students-tab.tsx
    - grep -q "breadthScore" apps/web/components/dashboard/students-tab.tsx
    - grep -q "Inactive" apps/web/components/dashboard/students-tab.tsx
    - grep -q "classes" apps/web/components/layout/sidebar.tsx
    - grep -q "dashboard" apps/web/components/layout/sidebar.tsx
    - grep -q "dashboard" apps/web/app/teacher/page.tsx
  </acceptance_criteria>
  <done>
    - Students tab renders a table with mini graph thumbnails, name, last active, streak, breadth score, and question count
    - Inactive students (3+ days) show amber "Inactive" badge
    - Breadth score shown as percentage with visual bar
    - Sidebar shows class-specific dashboard links for teacher
    - Teacher class list links to dashboard (not roster) as primary action
    - Roster remains accessible via secondary link
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL classId -> page | classId from URL is untrusted; getClassDashboardData already validates ownership (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | dashboard page | mitigate | Page calls getClassDashboardData which requires authenticated teacher session + class ownership (T-05-01 from Plan 01) |
| T-05-05 | Information Disclosure | sidebar class links | accept | Teacher can only see their own classes via getTeacherClasses (already filtered by session.user.id) |
</threat_model>

<verification>
1. Navigate to `/teacher/classes/{classId}/dashboard` — page renders with tabs
2. Overview tab shows 4 stat cards
3. Students tab shows table with graph thumbnails and engagement data
4. Sidebar shows class dashboard links for teacher role
5. Class list cards link to dashboard page
6. TypeScript compiles: `cd apps/web && npx tsc --noEmit`
</verification>

<success_criteria>
- Dashboard page renders with real data from getClassDashboardData
- 4 tabs visible, Overview and Students functional
- Students tab shows mini graph SVGs, inactivity badges, breadth scores
- Navigation updated: sidebar + class list point to dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/05-teacher-dashboard/05-02-SUMMARY.md`
</output>
